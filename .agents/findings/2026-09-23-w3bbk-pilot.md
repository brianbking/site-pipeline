# W3BBK.us pilot — platform findings (2026-09-23)

Answers to the plan's Task 10 questions, from PR brianbking/W3BBK#1 and the staging bootstrap.

1. **What 403s Node's `fetch` on the zones?** Not yet identified. Node `fetch` gets
   `cf-mitigated: challenge` on every family zone whatever its user agent; curl with a browser UA
   gets 200, so it keys on the client fingerprint (Bot Fight Mode is the likely cause). Open item:
   read Security → Events for one challenged request. The pipeline no longer depends on the answer,
   because every Node check runs against `workers.dev`.

2. **Do `_headers` rules apply to Worker responses from `env.ASSETS.fetch`?** Yes. On the real
   `w3bbk-staging` deploy, `/` and `/.well-known/agent-card.json` carry the `_headers` CSP, and the
   Worker's own `X-Robots-Tag` wins over the `_headers` value. `wrangler dev` behaves the same.

3. **Do preview URLs work with `workers_dev: false` + `preview_urls: true`?** Only once the
   Worker-level setting is on. `wrangler versions upload` does not apply `preview_urls` from config:
   the first run printed a Version ID and no preview URL. `w3bbk` needed Settings → Domains &
   Routes → Preview URLs → Enable in the dashboard. `wrangler deploy --env staging` did enable it
   for the new staging Worker. Every wave's setup checklist needs this toggle.

4. **Was a missing `run_worker_first` why `src/worker.js` never negotiated?** Yes, as far as the
   evidence goes. With `run_worker_first: true` the same kind of Worker negotiates on staging and on
   the PR preview. Wave 4 re-confirms on BrianBK.ing and JillK.ing.

5. **Real wrangler output (4.137):**
   - `versions upload` prints `Worker Version ID: <uuid>`, plus `Version Preview URL:` and
     `Version Preview Alias URL:` lines only when preview URLs are enabled.
   - `deployments status` prints `Version(s):  (100%) <uuid>`, which matched the parser.

6. **Are `workers.dev` preview URLs free of the zone challenge?** Yes. Node `fetch`, Playwright and
   Lighthouse all reached `https://pr-1-w3bbk.catastrophic.workers.dev` in the `live` job.

## Other findings

- **Preview URLs are forced `x-robots-tag: noindex` by the platform**, overriding the Worker's
  header. Lighthouse `is-crawlable` therefore fails on every preview, so v0.1.3 skips it.
- **Preview hosting distorts Best Practices.** Hugo emits absolute `https://w3bbk.us/...` asset URLs,
  and on the preview origin the site's CSP (`script-src 'self'`) blocks them, producing a console
  error. v0.1.3 judges BP and SEO against production (0 tolerance) instead of an absolute 95.
- **Lighthouse flags `robots.txt` as invalid** because of the `Content-Signal` / `Content-Usage`
  lines. This happens in production too, and it's accepted: the lines are intentional.
- **Visual regression uses pixelmatch `threshold: 0.1`,** which absorbed the footer opacity change
  0.6 → 0.7 as 0.000%. That's fine for layout regressions, but it's blind to subtle colour shifts.
- **npm writes `git+ssh` into `package-lock.json` for a GitHub git dependency**, even when
  `package.json` says `git+https`. `npm ci` still succeeds with SSH disabled, because pacote fetches
  GitHub dependencies as https tarballs. `npm install <url>` also rewrites `package.json` to the
  `github:` shorthand, so re-edit it afterwards.
- **Two `wrangler dev` processes in one project deadlock on `.wrangler/state` SQLite** (`SQLITE_BUSY`).
  Give the second one `--persist-to .wrangler/state-staging`.
- **W3BBK's baseline, now fixed:** Accessibility 87 → 100 after `.mastfoot` opacity 0.6 → 0.7.

## Wave 2 — BBKing.net (2026-09-23)

Live via PRs brianbking/BBKing#1 (→ `staging`) and #2 (→ `main`). Every gate is green, and the
visual diff was approved on both PRs. Production negotiates markdown (`X-Markdown-Tokens: 62`),
the staging dispatch path and the weekly run are green, and no issues are open.

- **Piping a secret into `gh secret set` under Windows PowerShell 5.1 stores a trailing CRLF.**
  The pilot plan's `op read … | gh secret set` snippet did this. wrangler then rejected
  `CLOUDFLARE_ACCOUNT_ID` ("Invalid account ID") in the preview job. Re-setting with
  `gh secret set NAME --body (op read …).Trim()` fixed it. Use `--body` in every later wave.
- **Production was behind `main` for four months.** Workers Builds last deployed before `90fedb5`
  (2026-05-27), so that commit, `5501e84` (negotiation) and `cf07a2e` (related data) only reached
  production at this cutover. Check `git log` against the live build before each wave.
- **The preview origin distorts visual regression as well as Best Practices.** `scripts.js` is loaded
  from the absolute URL `https://bbking.net/js/scripts.js`, and the preview origin's CSP
  (`script-src 'self'`) blocks it. On the old production build, that script is the only thing that
  sets `data-bs-theme`, so the baseline rendered without the `[data-bs-theme=light]` heading
  text-shadow (0.48 % / 0.54 %). Each PR compared against the same stale baseline, so the
  promotion PR needed the label again. The fix is at the source: use relative asset URLs.
- **Non-interactive `wrangler` needs `CLOUDFLARE_API_TOKEN`.** The staging bootstrap and
  `versions upload` checks therefore run in Brian's own terminal. The new `staging.<host>` custom
  domain returned NXDOMAIN from 1.1.1.1 for a few minutes after deploy.
- **Hugo's `.hugo_build.lock` was tracked** even though `.gitignore` listed it. It is untracked now.
  `static/humans.txt` still said "Hosted on: Cloudflare Pages": leftover greps must cover `static/`.
- **site-pipeline's `package.json` `version` has stayed `0.1.0` since v0.1.0,** so `npm ls` shows
  0.1.0 whatever the tag. Bump it with the next tag.
- **Actions minutes, 2026-09-01 → 23:** about 96 billable minutes (W3BBK ≈ 72, BBKing ≈ 24),
  counted as job durations rounded up per job, and including pilot iteration. That is well under the
  spec's ~700/month estimate. `settings/billing/actions` needs the `user` token scope, and
  `runs/{id}/timing` reports 0.

## Wave 3 — KingFamily (2026-09-23/24)

site-pipeline `v1.1.0` adds the Formspree offline check and the weekly canary. KingFamily went through
PRs brianbking/KingFamily#1 (→ `staging`) and #2 (→ `main`). The visual diff was approved on both,
and every gate was green. Production negotiates markdown (`X-Markdown-Tokens: 155`), and the
Dependabot alerts went from 16 to 0. `v1.1.1` (PRs #4/#5) excludes Formspree form endpoints from
the weekly link check, because a POST-only URL answers GET with 405. The weekly security.txt check
and the canary pass. Link-check issue #3 stays open for 3 referral links (content).

- **Formspree refuses JSON posts when reCAPTCHA is on.** `mgeggkzw` answered `400 {"error":"Please
  complete the reCAPTCHA"}`, while an unknown ID answers `404 FORM_NOT_FOUND`. Brian kept reCAPTCHA
  on, so the canary treats that refusal as proof the form exists and is enabled. It sends no email,
  and it can't prove delivery.
- **`hugo config --format json` lowercases param keys** (`.params.formspreeid`). This works on the
  runner's Hugo 0.162, and on all six sites locally.
- **Production was current:** the live pages differed from a local build of `main` only by Cloudflare's
  injected challenge script. That is unlike BBKing.
- **`staging.kingfamily.info` already had an Access application,** and the zone has a **wildcard** DNS
  record. `wrangler deploy --env staging` still created the custom domain without conflict.
- **A11y was 93 / 85 / 92.** It is now 100 on all three pages after fixing the empty link, the missing
  main landmark, the hidden breadcrumb label, `tabindex` > 0, the 18 px nav targets, and the dark-mode
  button contrast. `contact-form.html` is shared with BrianBK.ing and JillK.ing, so wave 4 inherits the
  `tabindex` fix. Turning the brand `<a>` into a bare `<span>` made it a flex item and shifted the
  whole home page (1.67 % visual diff); keeping the wrapper span fixed it.
- **The Lighthouse Performance gate is noisy on third-party-heavy pages.** `/referrals/` scored
  69 / 73 / 62 / 75 across runs. Local Lighthouse showed 6–11 s stalls in which every in-flight CDN and
  Google Fonts request finished together, and it reproduced with no Worker in front. See the backlog.
- **`gh run rerun --failed` re-runs only `site-gate`,** because the `live` job's steps are
  `continue-on-error`, so the job counts as successful. It re-summarizes the old scores. Use a full
  `gh run rerun`.
- **The pre-commit hook stages `.agents/state/*.md` into every commit** (a known ISSUES entry). It was
  split into its own commit here.
- **`workflow_dispatch` needs the workflow to have run once, or to be on the default branch.**
  `deploy.yml` could be dispatched on `staging` after its first push run; `weekly.yml` returns 404
  until it reaches `main`.
- **Dependabot has opened no site-pipeline pin PRs on W3BBK or BBKing** since `v1.0.0` (checked
  2026-09-24). Earlier pins were bumped by hand.
- **Workers Builds also builds non-production branches.** The `staging` merge commit `5d6326e` carries a
  failed `Workers Builds: kingfamily` check (`build.sh` is gone), next to the green Actions deploy.
  Disconnect it before the `main` merge, or `main` gets the same red check, or worse, a racing deploy.
